<nav class="nav navbar-default navbar-inverse navbar-fixed-top main-navbar">
  <div class="container">
    <!-- for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#main-navbar-list" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="{{@blog.url}}">{{@blog.title}}</a>
    </div>
    <!-- for nav menu -->
    <div class="collapse navbar-collapse" id="main-navbar-list">
      <ul class="nav navbar-nav  navbar-right">
        {{#foreach navigation}}
          <li {{#if current}}class="active"{{/if}}><a href="{{url absolute="true"}}">{{label}}{{#if current}}<span class="sr-only">(current)</span>{{/if}}</a></li>
        {{/foreach}}
          <li><a href="{{@blog.url}}/rss/">RSS</a></li>
      </ul>
    </div>
  </div>
</nav>
